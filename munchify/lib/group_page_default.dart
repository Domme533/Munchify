import 'package:flutter/material.dart';

class GroupPageDefault extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Group Page'),
      ),
      body: Center(
        child: Text('Welcome to the Group Page!'),
      ),
    );
  }
}